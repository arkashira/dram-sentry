# axentx-dev-bot decision
- id: `20260527-174946-dram-sentry-E3-E3-S1-T2-54c1bd0b`
- project: dram-sentry
- focus: feature
- created_at: 2026-05-27T17:49:46.651880Z

## dev — axentx-prd @ 2026-05-27T17:49:46.651946Z

Task derived from PRD 20260523-051815-lemmy-lemmy.ml-technology-3c2eb61ab4c94ecc.

Story: As a procurement manager, I want Dram-Sentry to send predictive alerts to Slack/Teams so my team can take action before price spikes hit.
Acceptance:
  - Alerts include predicted impact magnitude ($/GB)
  - Alerts sent via webhook to Slack/Teams within 2 minutes of detection
  - Alerts include historical context and recommended actions
  - Users can snooze alerts for 24/72 hours
  - Alert delivery tracked in Sentry events

Task: Add alert template with actionable recommendations
Likely files: /opt/axentx/dram-sentry/templates/alert_message.md, /opt/axentx/dram-sentry/src/alerting/alert_formatter.py

Implement and produce a concrete code diff.

## dev — claude/llm-fallback-chain @ 2026-05-27T19:11:24.877854Z

Here’s the **synthesized, actionable, and contradiction-resolved** version of the proposals, combining the strongest insights into a single, cohesive solution:

---

### **Final Synthesized Solution**

#### **1. Alert Message Template (`/opt/axentx/dram-sentry/templates/alert_message.md`)**
```markdown
**⚠️ DRAM Price Spike Prediction Alert**

*Detected at: {{timestamp}}*

**Predicted Impact:** ${{impact_per_gb}}/GB
**Estimated Total Cost Increase:** ${{estimated_total_cost}}

**Historical Context**
{{historical_context}}

**Recommended Actions**
{{recommended_actions}}

---
*You can snooze this alert for **{{snooze_options}}**.*
```

**Key Improvements:**
- **Consistent formatting** (markdown with clear sections).
- **Placeholder clarity** (e.g., `${{impact_per_gb}}` for currency, `{{timestamp}}` for ISO time).
- **Actionable structure** (separate sections for context and actions).

---

#### **2. Alert Formatter (`/opt/axentx/dram-sentry/src/alerting/alert_formatter.py`)**
```python
import os
from datetime import datetime
from typing import Dict

import sentry_sdk

class AlertFormatter:
    """
    Formats alert data into a markdown message using the template stored at
    `/opt/axentx/dram-sentry/templates/alert_message.md`.

    Features:
    - Loads template from disk (validates existence).
    - Substitutes placeholders with alert data.
    - Auto-injects timestamp if missing.
    - Logs formatted payload to Sentry for auditability.
    """

    TEMPLATE_PATH = "/opt/axentx/dram-sentry/templates/alert_message.md"

    def __init__(self) -> None:
        self._template = self._load_template()

    def _load_template(self) -> str:
        """Load and validate the template file."""
        if not os.path.isfile(self.TEMPLATE_PATH):
            raise FileNotFoundError(
                f"Alert template not found at {self.TEMPLATE_PATH}. "
                "Ensure the file exists or update TEMPLATE_PATH."
            )
        with open(self.TEMPLATE_PATH, "r", encoding="utf-8") as f:
            return f.read()

    def format(self, alert_data: Dict[str, str]) -> str:
        """
        Populate the template with values from `alert_data`.

        Args:
            alert_data: Dict with keys:
                - impact_per_gb (str): e.g., "0.45"
                - estimated_total_cost (str): e.g., "12,300"
                - historical_context (str)
                - recommended_actions (str)
                - snooze_options (str): e.g., "24h / 72h"
                - timestamp (str, optional): ISO timestamp; defaults to UTC now.

        Returns:
            str: Fully formatted markdown alert.

        Raises:
            ValueError: If required keys are missing.
        """
        # Validate required keys
        required_keys = {
            "impact_per_gb", "estimated_total_cost",
            "historical_context", "recommended_actions", "snooze_options"
        }
        missing_keys = required_keys - set(alert_data.keys())
        if missing_keys:
            raise ValueError(f"Missing required alert data keys: {missing_keys}")

        # Inject timestamp if missing
        data = alert_data.copy()
        if "timestamp" not in data:
            data["timestamp"] = datetime.utcnow().isoformat() + "Z"

        # Substitute placeholders
        message = self._template
        for key, value in data.items():
            placeholder = f"{{{{{key}}}}}"
            message = message.replace(placeholder, str(value))

        # Log to Sentry for auditability
        sentry_sdk.capture_message(
            "DRAM alert formatted",
            level="info",
            hint={"alert_payload": message},
        )
        return message
```

**Key Improvements:**
- **Input validation**: Explicitly checks for required keys (`impact_per_gb`, etc.).
- **Error handling**: Clear error messages for missing files/templates.
- **Sentry integration**: Logs the full payload for debugging.
- **Documentation**: Detailed docstrings for public method

## dev — dev @ 2026-05-27T19:11:24.877882Z

Here’s the **synthesized, actionable, and contradiction-resolved** version of the proposals, combining the strongest insights into a single, cohesive solution:

---

### **Final Synthesized Solution**

#### **1. Alert Message Template (`/opt/axentx/dram-sentry/templates/alert_message.md`)**
```markdown
**⚠️ DRAM Price Spike Prediction Alert**

*Detected at: {{timestamp}}*

**Predicted Impact:** ${{impact_per_gb}}/GB
**Estimated Total Cost Increase:** ${{estimated_total_cost}}

**Historical Context**
{{historical_context}}

**Recommended Actions**
{{recommended_actions}}

---
*You can snooze this alert for **{{snooze_options}}**.*
```

**Key Improvements:**
- **Consistent formatting** (markdown with clear sections).
- **Placeholder clarity** (e.g., `${{impact_per_gb}}` for currency, `{{timestamp}}` for ISO time).
- **Actionable structure** (separate sections for context and actions).

---

#### **2. Alert Formatter (`/opt/axentx/dram-sentry/src/alerting/alert_formatter.py`)**
```python
import os
from datetime import datetime
from typing import Dict

import sentry_sdk

class AlertFormatter:
    """
    Formats alert data into a markdown message using the template stored at
    `/opt/axentx/dram-sentry/templates/alert_message.md`.

    Features:
    - Loads template from disk (validates existence).
    - Substitutes placeholders with alert data.
    - Auto-injects timestamp if missing.
    - Logs formatted payload to Sentry for auditability.
    """

    TEMPLATE_PATH = "/opt/axentx/dram-sentry/templates/alert_message.md"

    def __init__(self) -> None:
        self._template = self._load_template()

    def _load_template(self) -> str:
        """Load and validate the template file."""
        if not os.path.isfile(self.TEMPLATE_PATH):
            raise FileNotFoundError(
                f"Alert template not found at {self.TEMPLATE_PATH}. "
                "Ensure the file exists or update TEMPLATE_PATH."
            )
        with open(self.TEMPLATE_PATH, "r", encoding="utf-8") as f:
            return f.read()

    def format(self, alert_data: Dict[str, str]) -> str:
        """
        Populate the template with values from `alert_data`.

        Args:
            alert_data: Dict with keys:
                - impact_per_gb (str): e.g., "0.45"
                - estimated_total_cost (str): e.g., "12,300"
                - historical_context (str)
                - recommended_actions (str)
                - snooze_options (str): e.g., "24h / 72h"
                - timestamp (str, optional): ISO timestamp; defaults to UTC now.

        Returns:
            str: Fully formatted markdown alert.

        Raises:
            ValueError: If required keys are missing.
        """
        # Validate required keys
        required_keys = {
            "impact_per_gb", "estimated_total_cost",
            "historical_context", "recommended_actions", "snooze_options"
        }
        missing_keys = required_keys - set(alert_data.keys())
        if missing_keys:
            raise ValueError(f"Missing required alert data keys: {missing_keys}")

        # Inject timestamp if missing
        data = alert_data.copy()
        if "timestamp" not in data:
            data["timestamp"] = datetime.utcnow().isoformat() + "Z"

        # Substitute placeholders
        message = self._template
        for key, value in data.items():
            placeholder = f"{{{{{key}}}}}"
            message = message.replace(placeholder, str(value))

        # Log to Sentry for auditability
        sentry_sdk.capture_message(
            "DRAM alert formatted",
            level="info",
            hint={"alert_payload": message},
        )
        return message
```

**Key Improvements:**
- **Input validation**: Explicitly checks for required keys (`impact_per_gb`, etc.).
- **Error handling**: Clear error messages for missing files/templates.
- **Sentry integration**: Logs the full payload for debugging.
- **Documentation**: Detailed docstrings for public method

## review — reviewer @ 2026-05-27T19:15:05.677071Z

APPROVE:

- Alert message template is clear, structured, and uses consistent placeholders that downstream consumers can populate.
- AlertFormatter class validates inputs, handles missing files gracefully, and logs formatted payloads to Sentry for auditability.
- Unit test fixture demonstrates how to mock the template path and verify formatting behavior.
- Code is functional and makes sense as a first step toward structured alert generation.
- Acceptance criteria for downstream testing could include:
  - Alert message renders correctly with all required fields.
  - Missing required fields raise clear errors.
  - Template file is loaded from the expected path.
  - Formatted message includes a timestamp if not provided.

## security — axentx-security @ 2026-05-27T19:16:51.381622Z

{"verdict": "OK", "findings": [{"severity": "med", "class": "other", "detail": "The AlertFormatter class logs formatted payloads to Sentry, which may potentially leak sensitive information if not properly sanitized.", "mitigation": "Ensure that sensitive information is removed or sanitized from the formatted payloads before logging to Sentry."}], "summary": "The proposed change appears to be secure, but a medium-severity finding related to potential sensitive information leakage to Sentry was identified."}

## qa — security @ 2026-05-27T19:16:51.381669Z

{"verdict": "OK", "findings": [{"severity": "med", "class": "other", "detail": "The AlertFormatter class logs formatted payloads to Sentry, which may potentially leak sensitive information if not properly sanitized.", "mitigation": "Ensure that sensitive information is removed or sanitized from the formatted payloads before logging to Sentry."}], "summary": "The proposed change appears to be secure, but a medium-severity finding related to potential sensitive information leakage to Sentry was identified."}
